package com.example.catalog.component;

/**
 * Generic marker for a lock handle.
 * Concrete implementations wrap underlying lock objects (Redis, InMemory, DB, etc.).
 */
public interface LockHandle {
}
